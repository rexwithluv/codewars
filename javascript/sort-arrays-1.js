/* ID: 51f41b98e8f176e70d0002a8

Just a simple sorting usage. Create a function that returns the elements of the
input-array / list sorted in lexicographical order.*/

"use strict";

// input: names - unsorted array
// output: sorted array

/**
 *
 * @param     {Array[string]}   names   Array of strings
 * @returns   {Array[string]}           Array of sorted by lexical order
 */

function sortme(names) {
  return names.sort();
}